package com.melodytune.backend.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.ResourceHandlerRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
public class WebMvcConfig implements WebMvcConfigurer {

    // 실제 이미지가 저장될 로컬 경로 (끝에 / 꼭 붙여주세요)
    // 윈도우 예시: "file:///C:/melody_images/"
    // 맥/리눅스 예시: "file:///Users/username/melody_images/"
    private String uploadPath = "file:///C:/melody_images/";

    @Override
    public void addResourceHandlers(ResourceHandlerRegistry registry) {
        // 웹 브라우저에서 http://localhost:8080/images/abc.jpg 로 요청하면
        // 실제 로컬의 C:/melody_images/abc.jpg 파일을 보여준다.
        registry.addResourceHandler("/images/**")
                .addResourceLocations(uploadPath);
    }
}